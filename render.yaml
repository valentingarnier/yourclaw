services:
  - type: web
    name: yourclaw-api
    runtime: python
    rootDir: services/api
    buildCommand: pip install uv && uv sync --frozen
    startCommand: uv run uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.0"

  - type: worker
    name: yourclaw-worker
    runtime: python
    rootDir: services/api
    buildCommand: pip install uv && uv sync --frozen
    startCommand: uv run python -m app.worker
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.0"
